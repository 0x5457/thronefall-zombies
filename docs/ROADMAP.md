# PINEFALL 开发路线图

> 本文是可执行的开发计划与任务拆分；具体数值以 `src/rules.ts` 为运行时唯一来源。
> 状态：TODO / DOING / PARTIAL / VERIFY / DONE / BLOCKED（在 `DEVELOPMENT.md` 跟踪）。
> 设计目标变更先改 `GAME_DESIGN.md`，再改本文任务，最后动代码。

## 0. 产品形态（决定）

- **形态：Thronefall 式短局重玩**。一局 5 夜、约 20–30 分钟；靠"评分 + 恶兆 + 解锁 + 构筑组合"产生重复可玩性，而不是靠线性剧情拉长。
- **核心幻想**：白天用有限资源把营地变成防线，夜晚亲自守住它，天亮时知道自己为什么赢/为什么输。
- **核心体验目标**：每一夜都是一道可预判、需要构筑解答的战术题；失败能解释，重开能改进，改进能变强。
- **暂不采用**：开放世界、多人、程序化无限内容、大型剧情战役（作为发行后扩展方向）。

## 1. 现状诊断（为什么像 demo）

| 问题 | 证据 | 对应任务 |
|---|---|---|
| 开局没有目标，不知道怎么上手 | 首日引导缺失；`WAVES[].hint` 无消费者；150 秒倒计时到点直接开战 | WLD-03a/b/c |
| 白天动词太弱，采集是瞬时加数字 | 按 E 无动作/无音效冲击/倒木无变化，只有一条 toast | FBK-01 |
| 升级外观变化太小，看不出塔变强 | T8 只加小横梁/第二旗，默认镜头不可辨，无等级标识 | WLD-02 |
| 夜晚没有题面：敌人只是走固定路线的数值块 | 夜 1 仅 10 游荡者；行为只有速度/护甲差异 | NGT-01/02 |
| 资源不缺，选择不痛 | 开局 25 木 + 每日 25；塔 35、栅栏 15、灯 10，"不能全都要"已由 ECO-01 收紧（待用户实机确认手感） | ECO-01/02/03 |
| 情报看了没用 | 情报页只有组成与数量，缺"该带什么/该防哪里" | NGT-03 |
| 建筑只有数值升级 | 升级 = +血 +伤害，没有分支与协同 | BLD-01/02 |
| 玩家操作空间小 | 自动射击 + 固定路线，走位收益低 | SKL-01/02/03 |
| 失败无痛、没有重玩理由 | 倒地只扣 15 营地；无评分、无变体、无跨局解锁 | META-01/02/03 |
| 世界只有营地 | 外围 180×180 无地标；远征是菜单数字 | WLD-01/02 |
| 功能改动后没有统一回归 | 浏览器测试按主题分散，改 HUD/引导容易漏测 | TST-01 |

## 2. 阶段与任务

任务 ID 规则：`阶段前缀-序号`。每项任务必须能实际操作、验证，并有明确验收。

### Phase 0 · 状态架构（ARC）— 统一状态与存档前置
设计见 `docs/STATE_MACHINE_DESIGN.md`；依赖决策见 `GAME_DESIGN.md` §10。

| ID | 任务 | 验收标准 | 状态 |
|---|---|---|---|
| ARC-01 | 显式状态机：XState 状态图与 rules 集成骨架（不接 main.ts） | 合法/非法转移、guards、phase 同步有 Node 单测；现有行为不变 | VERIFY |
| ARC-02 | main.ts 接入 actor：布尔标志迁移到 `snapshot.matches` | 现有浏览器回归全过；UI/音频由订阅驱动，行为不变 | VERIFY |
| ARC-03 | 权威数据化：建筑/倒木/进度进入 state，场景改 id→mesh | 状态可 JSON 往返；建造/升级/拆除/读档重建浏览器验证 | VERIFY |
| ARC-04 | 存档模块与黎明检查点（T6） | 黎明写入、刷新续玩、坏档回退、高版本拒绝且不覆写 | VERIFY |
| ARC-05 | 存档回退边界：current 为合法 JSON 但 schema/迁移失败时继续尝试 backup | current 非法字段 + backup 合法时读回 backup（`recovered=true`）；仅两槽都不可用才 corrupt；坏档仍不覆写；高版本拒绝语义不变；`tests/save.test.ts` 覆盖 | DONE |

### Phase 1 · 夜间有题（NGT）— 本里程碑最高优先
| ID | 任务 | 验收标准 | 状态 |
|---|---|---|---|
| NGT-01 | 五夜课题化：`WAVES` 增加 `lesson`/`intent`/`advice`，重排组队与呼吸 | 五夜各有独立课题与组队；规则单测覆盖；情报页可见 | VERIFY |
| NGT-02 | 敌人意图行为：破阵者优先拆塔、远距离吐息者、雾夜潜行者 | 三类行为在实战可观察；浏览器测试覆盖至少两类 | VERIFY |
| NGT-03 | 情报页升级：课题、意图标签、建议、昼夜能见度预告 | 打开情报能看到"今晚是什么题"；数据驱动自 `rules.ts` | VERIFY |
| NGT-04 | 夜晚环境差异：雾/能见度 | 迷雾影响月光/提灯/营地灯半径与潜行者显形，情报预告 | VERIFY |
| NGT-05 | 战前确认界面：白天结束先进入"今晚情报 + 最后确认"，可返回白天或确认开战 | `elapsed>=DAY_LENGTH` 不再直接 `START_NIGHT`；模态显示今晚波次/路线/特殊敌人/建议与当前准备摘要，冻结预算与输入，键盘可操作；返回/确认两条路径有浏览器测试，非法状态由 machine 拒绝；同步更新 `STATE_MACHINE_DESIGN.md` | DONE（N/按钮保持直接入夜；`tests/night-confirm.browser.ts` PASS；用户已确认） |
| NGT-06 | 风向游戏性（原 NGT-04 后半）：弹道与火焰受风影响并在情报预告 | 先在设计文档明确取舍（保留则风向入 `rules.ts`、投射物/火焰受风、情报显示；不做则从路线图删除）；规则单测 + 浏览器可观察 | DONE（设计已落 `GAME_DESIGN.md` §6；`tests/wind.*` PASS；用户已确认） |

### Phase 2 · 稀缺与取舍（ECO）
| ID | 任务 | 验收标准 | 状态 |
|---|---|---|---|
| ECO-01 | 经济重平衡：收入、成本、黎明结算收紧到"不能全都要" | 单测给出五夜预算曲线；实战验证前两夜必须取舍；初始值按 `GAME_DESIGN.md` §5：新局 25 木 / 6 零件、黎明 +25 木 / +5 零件、倒木 3×10 木 | VERIFY |
| ECO-02 | 白天时间压力：90–120s、采集冷却、远征占用预算 | 情报/结算明示；实测采集/远征不能全做 | TODO |
| ECO-03 | 建造位上限：分类上限（塔/栅栏/灯）与剩余位 UI | 超限有原因提示；情报与建造栏显示剩余 | TODO |
| ECO-04 | 修复与耐久收紧：修复只在白天、价格随等级、营地不回满 | 漏怪的后果跨夜可见；单测覆盖 | TODO |

### Phase 3 · 构筑深度（BLD）
| ID | 任务 | 验收标准 | 状态 |
|---|---|---|---|
| BLD-01 | 三级升级分支：塔（速射/穿透/溅射）、灯（范围/显形/引燃）、栅栏（荆棘/加固/连墙） | 升到 2/3 级出现分支选择 UI；模型/特效有变化 | TODO |
| BLD-02 | 建筑协同：灯照塔暴击、相邻栅栏减伤、塔互相供弹 | 协同在单测与实战均生效；情报/面板有说明 | TODO |
| BLD-03 | 建造面板展示分支与协同 | 点击建筑能看到当前分支、效果与升级预览 | TODO |
| BLD-04 | 建筑阻挡形状与旋转模型一致（撤销圆形近似） | 放置预览、敌人拆墙判定、角色/敌人碰撞共用同一旋转形状与角度；浏览器验证旋转 90° 后实际阻挡与预览一致 | DONE（`FOOTPRINTS` + `tests/footprint.test.ts`/`bld.browser.ts`；用户已确认） |
| BLD-05 | 建造通行性校验（防自锁） | 放置前检查未封死玩家/敌人通道；非法时显示具体原因；单测覆盖典型封死场景 + 浏览器验证 | DONE（`sealsPlayer()` 洪泛 + 拒绝原因；浏览器 PASS；用户已确认） |

### Phase 4 · 技巧空间（SKL）
| ID | 任务 | 验收标准 | 状态 |
|---|---|---|---|
| SKL-01 | 目标优先级：在最近/最硬/首领/拆墙者间切换（`Tab` 或面板） | HUD 显示当前优先级；实战可感知差异 | TODO |
| SKL-02 | 冲刺交互：穿过敌人造成击退/短暂破甲 | 冲刺实战有用；单测覆盖碰撞与体力 | TODO |
| SKL-03 | 信号弹进化：引燃/恐慌（短暂失控），而不只是减速 | 火焰持续伤害/恐慌行为可观察；冷却与范围一致 | TODO |
| SKL-04 | 抢修交互：靠近建筑 `E` 快速修复（有暴露风险） | 战斗中可抢修，有冷却/代价；面板提示 | TODO |

### Phase 5 · 重玩与成长（META）
| ID | 任务 | 验收标准 | 状态 |
|---|---|---|---|
| META-01 | 黎明评分：营地耐久、耗时、零损、资源 → S/A/B/C | 黎明结算展示评分与明细；单测覆盖 | VERIFY |
| META-02 | 恶兆修正：每夜可选，提高难度/奖励 | 至少 4 种恶兆；开局选择界面；单测覆盖 | TODO |
| META-03 | 解锁树：评分解锁武器/建筑分支/外观 | 解锁持久化；UI 明示条件 | TODO |
| META-04 | 检查点存档（T6）：版本化 JSON + 损坏/版本处理 | 黎明存档、重开续玩、坏档不覆写 | VERIFY（ARC-04 落地） |

### Phase 6 · 世界与情感（WLD）
| ID | 任务 | 验收标准 | 状态 |
|---|---|---|---|
| WLD-01 | 外围地标与资源点：把远征变成真实地点或可探索点 | 至少 3 个带交互的外围点位 | TODO |
| WLD-02 | 升级外观可读性：建筑等级在默认镜头下可辨 | 塔每级剪影变化（增高/平台/旗色/发光顶）+ 选中建筑世界内显示 1–3 等级点；灯与栅栏同级要求；同机位 1/2/3 级截图对照；无 UI 截图也能看出营地变强 | VERIFY |
| WLD-03a | 首日引导·数据与判定：引导步骤、完成判定、跳过开关 | 引导状态由 `state.logs`/`state.buildings`/`phase` 派生，不新增存档字段；`tests/guidance.test.ts` 覆盖正序与“先建塔”乱序；`WAVES[].hint` 被引导消费（FIX-02） | VERIFY |
| WLD-03b | 首日引导·目标卡与倒木高亮 | 目标卡按步骤显示文案与剩余；最近倒木世界内高亮 + 「E 采集」提示；第 2 天起不再出现；`tests/gather.browser.ts` 断言三步推进 | VERIFY |
| WLD-03c | 首日引导·首夜站位提示 | 入夜时提示“站到北径路口，别让它们靠近营地”；进入夜晚即完成；浏览器测试覆盖 | VERIFY |

### Phase 7 · 发行门槛（SHP）
桌面包、离线启动、设置与无障碍、本地化文本管理、崩溃报告、Steamworks/Cloud/成就、外部试玩 ≥5 人、设备矩阵、性能基线 T12——全部 TODO。详见 `GAME_DESIGN.md` §9。

### Phase 8 · 一致性与维护（FIX）— 2026-09-19 审计新增，不阻塞里程碑
| ID | 任务 | 验收标准 | 状态 |
|---|---|---|---|
| FIX-01 | 余火守望体力恢复：实现设计中的"体力恢复改善"，或修订设计为纯生命专长 | 二选一并落到代码/卡片/文档同一口径；规则单测覆盖；删除过时的"未接入"说明 | DONE（`staminaRegen/staminaDelay` +50%/−40%；`tests/survivor.test.ts`；用户已确认） |
| FIX-02 | 死数据与未引用清理：`WAVES[].hint`（5 条无消费者）、`MEDKIT_CAP`（未引用） | 被真实 UI 消费或删除；`vp check`/`vp test` 全绿 | DONE（`hint` 由 WLD-03a 引导消费；`MEDKIT_CAP` 接入 `maxMedkits` 并单测锁定；用户已确认） |
| FIX-03 | 文档与代码同步：`.js`→`.ts` 路径、敌人模型数量、过时的"未接入"表述 | `GAME_DESIGN`/`ROADMAP`/`STATE_MACHINE_DESIGN`/`RV_*`/`AGENTS.md` 不再引用不存在的文件或已实现功能的旧限制；`STATE_MACHINE_DESIGN` 头部"实现未开始"更新为已实现；不改写 DEVLOG 历史条目 | DONE（机械核对通过；`vp check` 全绿；用户已确认） |

### Phase 9 · 上手反馈（FBK）与回归（TST）— 2026-09-19 用户反馈新增，M1 前置
| ID | 任务 | 验收标准 | 状态 |
|---|---|---|---|
| FBK-01 | 采集闭环：砍伐动作、倒木状态变化、木质粒子、砍击音效 | 0.45s 动作（0.28s 结算）、期间锁移动与重复采集、0.35s 冷却；倒木逐次缩短、采空剩树桩、黎明恢复；`GATHER` 数值进 `rules.ts` 并单测；`tests/gather.browser.ts` 断言动作/冷却/剩余/恢复；截图对照 | VERIFY |
| FBK-02 | 采集反馈的 reduced-motion 与暂停安全 | 暂停/模态时冻结动作与冷却；`prefers-reduced-motion` 下缩短镜头/粒子但不吞结算；单测或浏览器覆盖 | VERIFY |
| TST-01 | 浏览器测试统一入口扩展：`npm run test:browser` 覆盖本阶段新增用例 | `gather` 与更新后的 `campaign`/`victory`/`audio` 纳入 runner；文档记录需要 dev server 的用例；不引入新依赖 | VERIFY |

## 3. 里程碑

1. **M1 可玩性拐点（当前）**：Phase 1 全部（NGT-05 战前确认纳入；NGT-06 风向可延后至 M2）+ ECO-01/03 + META-01 + Phase 9（FBK-01/02、WLD-03a/b/c、WLD-02、TST-01）。验收：前两夜需要真实取舍与针对性准备，玩家能说出"我为什么输"；新玩家 3 分钟内完成首个建设目标，采集与升级有可感知反馈。
2. **M2 重玩拐点**：Phase 2 剩余 + Phase 3 + META-02。验收：同一夜换构筑/恶兆有不同解法。
3. **M3 手感与技巧**：Phase 4 + WLD-01。验收：玩家操作能显著改变战局。
4. **M4 世界与叙事**：Phase 6。验收：营地与外围有可探索内容与情感回报。
5. **M5 发行**：Phase 7 全部。

## 4. 每任务执行流程

1. 读 `GAME_DESIGN.md` 相关章节 → 必要时先改设计。
2. 规则改动先写 `src/rules.ts` + `tests/*.test.ts`（Node 内置测试）。
3. 场景/UI 接入后跑 `npm test`、`npm run build`，再跑相关浏览器测试（`tests/*.browser.ts`），必要时新增。
4. 更新 `docs/DEVELOPMENT.md`（状态/证据/下一步）与 `docs/DEVLOG.md`。
5. 画面改动保存同视角昼夜截图到 `artifacts/`；未实测不宣称帧率。
